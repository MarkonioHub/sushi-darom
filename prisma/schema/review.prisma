model Review {
  id String @id @default(cuid())
  name String
  surname String?
  phone String
  email String?
  city String
  theme String
  message String
  file String?

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt
  @@map("reviews")
}