model Category {
  id String @id @default(cuid())
  name String
  slug String @unique
  image String
  products Product[]
  sortOrder Int @default(0)
  isActive  Boolean @default(true)
  @@map("categories")
}